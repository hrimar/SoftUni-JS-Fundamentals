function f(arr) { // 100/100
    let str = arr.join('');
    let result = str.split('').reverse().join(''); // REVERS OF STRING !
    console.log(result);
}

f(['I', 'am', 'student']);
console.log('');
f(['race', 'car']);
