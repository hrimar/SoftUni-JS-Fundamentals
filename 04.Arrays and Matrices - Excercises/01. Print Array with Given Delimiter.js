function delimiterArray(arr) {  // 100/100
    // // var.1:
    // // let delimeter = arr.slice(-1); or
    // let delimeter = arr[arr.length-1] or
    //
    // let newArr = arr.slice(0, arr.length - 1);
    //
    //  console.log(newArr.join(delimeter));

    // var.2:
    let delimeter = arr.pop();
    arr.
    console.log(arr.join(delimeter))
}

delimiterArray(['One', 'Two', 'Three', 'Four', 'Five', '-'])