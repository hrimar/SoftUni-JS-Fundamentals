function f(str, surch) { //100/100
    let result = str.endsWith(surch)
    console.log(result);
}

f('This sentence ends with fun?',
    'fun?')
console.log('');
f('This is Houston, we have…',
    'We have…')
console.log('');